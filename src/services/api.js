import axios from 'axios'
import { server } from '@/services/constants'
import router from '@/router'

const login = async values => {
  //server.LOGIN_URL -> `login`
  let result = await axios.post()
}